var length = prompt("What is the length of the rectangle?");
var height = prompt("What is the height of the rectangle?");
var width = prompt("What is the width of the rectangle?");

var volume = Number(length)*Number(height)*Number(width);

console.log("The length is " +length+ ", the height is " +height+ " and the width is " +width+ ". The volume of the rectangle is " +volume+ ".");